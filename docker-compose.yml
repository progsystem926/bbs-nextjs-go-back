version: "3.8"
services:
  app:
    container_name: app
    build:
      context: .
      dockerfile: ./build/docker/go/Dockerfile
    tty: true
    ports:
      - "8080:8080"
    volumes:
      - ./:/app
